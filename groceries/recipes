import React, { useMemo, useState } from "react";
import { Search, ArrowLeft, Clock, Users, Tag, ExternalLink, ShoppingCart } from "lucide-react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

const recipes = [
  {
    id: "lemon-butter-chicken-sheet-pan",
    name: "Trader Joe's Lemon Butter Chicken Sheet Pan",
    category: "Dinner",
    source: "Frida Sofia Eats",
    sourceUrl: "https://www.fridasofiaeats.com/recipes/lemon-butter-chicken-sheet-pan",
    servings: 4,
    prepTime: "10 min",
    cookTime: "25 min",
    tags: ["Trader Joe's", "Sheet Pan", "Chicken", "Easy"],
    description: "A simple weeknight sheet pan dinner built around Trader Joe's lemon butter flavors, chicken, and vegetables.",
    ingredients: [
      "Chicken breast or thighs",
      "Trader Joe's lemon butter or lemon garlic sauce",
      "Baby potatoes",
      "Green beans or broccoli",
      "Olive oil",
      "Salt and pepper",
      "Lemon wedges"
    ],
    instructions: [
      "Preheat oven to 400°F.",
      "Add chicken and vegetables to a sheet pan.",
      "Toss with lemon butter sauce, olive oil, salt, and pepper.",
      "Bake until the chicken is cooked through and the vegetables are tender.",
      "Serve with lemon wedges."
    ],
    notes: "Good option for a low-effort dinner night. Add rice or orzo if you want it more filling."
  },
  {
    id: "french-onion-chicken-orzo",
    name: "Trader Joe's French Onion Chicken Orzo",
    category: "Dinner",
    source: "Frida Sofia Eats",
    sourceUrl: "https://www.fridasofiaeats.com/recipes/french-onion-chicken-orzo",
    servings: 4,
    prepTime: "10 min",
    cookTime: "25 min",
    tags: ["Trader Joe's", "Chicken", "Orzo", "Comfort Food"],
    description: "A cozy chicken and orzo dinner with French onion flavor, broth, cheese, and a creamy finish.",
    ingredients: [
      "Chicken breast",
      "Orzo pasta",
      "French onion soup or onion broth",
      "Garlic",
      "Butter",
      "Parmesan or Gruyere",
      "Spinach or herbs"
    ],
    instructions: [
      "Cook chicken in a large skillet until browned.",
      "Add garlic, orzo, and broth.",
      "Simmer until the orzo is tender.",
      "Stir in cheese and greens.",
      "Serve warm."
    ],
    notes: "This is a good one-pot dinner candidate. Keep extra broth nearby if the orzo absorbs too much liquid."
  },
  {
    id: "spinach-artichoke-orzo",
    name: "Trader Joe's Spinach and Artichoke Orzo",
    category: "Dinner / Vegetarian",
    source: "Frida Sofia Eats",
    sourceUrl: "https://www.fridasofiaeats.com/recipes/spinach-and-artichoke-orzo",
    servings: 4,
    prepTime: "10 min",
    cookTime: "20 min",
    tags: ["Trader Joe's", "Vegetarian", "Orzo", "Creamy"],
    description: "A creamy vegetarian orzo dish with spinach, artichokes, cream cheese, Parmesan, and lemon.",
    ingredients: [
      "Orzo pasta",
      "Garlic",
      "Butter",
      "Olive oil",
      "Vegetable broth",
      "Fresh spinach",
      "Artichoke hearts",
      "Cream cheese",
      "Parmesan cheese",
      "Lemon juice"
    ],
    instructions: [
      "Cook garlic in butter and olive oil.",
      "Add orzo and vegetable broth.",
      "Simmer until orzo is tender.",
      "Stir in spinach, artichokes, cream cheese, Parmesan, and lemon juice.",
      "Serve once creamy and warmed through."
    ],
    notes: "Can be served as a vegetarian main or as a side with chicken."
  },
  {
    id: "salsa-verde-chicken-couscous",
    name: "Trader Joe's Salsa Verde Chicken and Couscous",
    category: "Dinner",
    source: "Frida Sofia Eats",
    sourceUrl: "https://www.fridasofiaeats.com/recipes/salsa-verde-chicken-and-couscous",
    servings: 4,
    prepTime: "10 min",
    cookTime: "20 min",
    tags: ["Trader Joe's", "Chicken", "Couscous", "Quick"],
    description: "A quick chicken dinner using salsa verde, couscous, and simple toppings.",
    ingredients: [
      "Chicken breast",
      "Salsa verde",
      "Couscous",
      "Chicken broth",
      "Corn",
      "Black beans",
      "Lime",
      "Cilantro",
      "Cheese or avocado"
    ],
    instructions: [
      "Cook chicken in a skillet.",
      "Add salsa verde and simmer.",
      "Prepare couscous with broth.",
      "Combine chicken, couscous, corn, and beans.",
      "Top with lime, cilantro, cheese, or avocado."
    ],
    notes: "Good for leftovers and easy to customize."
  },
  {
    id: "soyaki-beef-bowl",
    name: "Trader Joe's Soyaki Beef Bowl",
    category: "Dinner",
    source: "Frida Sofia Eats",
    sourceUrl: "https://www.fridasofiaeats.com/recipes/soyaki-beef-bowl",
    servings: 4,
    prepTime: "10 min",
    cookTime: "15 min",
    tags: ["Trader Joe's", "Beef", "Rice Bowl", "Quick"],
    description: "A fast rice bowl with beef, vegetables, and Trader Joe's Soyaki sauce.",
    ingredients: [
      "Beef strips or ground beef",
      "Trader Joe's Soyaki sauce",
      "Rice",
      "Broccoli or stir-fry vegetables",
      "Green onions",
      "Sesame seeds",
      "Sriracha or chili crunch"
    ],
    instructions: [
      "Cook rice according to package directions.",
      "Brown beef in a skillet.",
      "Add vegetables and Soyaki sauce.",
      "Cook until vegetables are tender and sauce coats the beef.",
      "Serve over rice with green onions and sesame seeds."
    ],
    notes: "One of the fastest weeknight options."
  }
];

function RecipeCard({ recipe, onSelect }) {
  return (
    <button
      onClick={() => onSelect(recipe)}
      className="w-full text-left transition hover:-translate-y-0.5"
    >
      <Card className="h-full rounded-2xl shadow-sm hover:shadow-md">
        <CardContent className="p-5 space-y-3">
          <div className="flex items-start justify-between gap-3">
            <div>
              <h2 className="text-xl font-semibold leading-tight">{recipe.name}</h2>
              <p className="mt-1 text-sm text-slate-500">{recipe.category}</p>
            </div>
            <span className="rounded-full bg-slate-100 px-3 py-1 text-xs font-medium text-slate-700">
              {recipe.servings} servings
            </span>
          </div>
          <p className="text-sm leading-6 text-slate-600">{recipe.description}</p>
          <div className="flex flex-wrap gap-2">
            {recipe.tags.slice(0, 4).map((tag) => (
              <span key={tag} className="rounded-full border px-2.5 py-1 text-xs text-slate-600">
                {tag}
              </span>
            ))}
          </div>
        </CardContent>
      </Card>
    </button>
  );
}

function RecipeDetail({ recipe, onBack }) {
  return (
    <div className="mx-auto max-w-4xl space-y-6">
      <Button variant="ghost" onClick={onBack} className="gap-2">
        <ArrowLeft className="h-4 w-4" /> Back to recipes
      </Button>

      <Card className="rounded-2xl shadow-sm">
        <CardContent className="p-6 md:p-8 space-y-6">
          <div className="space-y-3">
            <p className="text-sm font-medium uppercase tracking-wide text-slate-500">{recipe.category}</p>
            <h1 className="text-3xl font-bold tracking-tight md:text-4xl">{recipe.name}</h1>
            <p className="text-slate-600 leading-7">{recipe.description}</p>
          </div>

          <div className="grid gap-3 sm:grid-cols-3">
            <div className="rounded-2xl bg-slate-50 p-4">
              <Users className="mb-2 h-5 w-5 text-slate-500" />
              <div className="text-sm text-slate-500">Servings</div>
              <div className="font-semibold">{recipe.servings}</div>
            </div>
            <div className="rounded-2xl bg-slate-50 p-4">
              <Clock className="mb-2 h-5 w-5 text-slate-500" />
              <div className="text-sm text-slate-500">Prep</div>
              <div className="font-semibold">{recipe.prepTime}</div>
            </div>
            <div className="rounded-2xl bg-slate-50 p-4">
              <Clock className="mb-2 h-5 w-5 text-slate-500" />
              <div className="text-sm text-slate-500">Cook</div>
              <div className="font-semibold">{recipe.cookTime}</div>
            </div>
          </div>

          <div className="flex flex-wrap gap-2">
            {recipe.tags.map((tag) => (
              <span key={tag} className="inline-flex items-center gap-1 rounded-full border px-3 py-1 text-sm text-slate-600">
                <Tag className="h-3.5 w-3.5" /> {tag}
              </span>
            ))}
          </div>

          <div className="grid gap-8 md:grid-cols-2">
            <section>
              <h2 className="mb-3 flex items-center gap-2 text-xl font-semibold">
                <ShoppingCart className="h-5 w-5" /> Ingredients
              </h2>
              <ul className="space-y-2">
                {recipe.ingredients.map((item) => (
                  <li key={item} className="rounded-xl bg-slate-50 px-4 py-2 text-slate-700">
                    {item}
                  </li>
                ))}
              </ul>
            </section>

            <section>
              <h2 className="mb-3 text-xl font-semibold">Instructions</h2>
              <ol className="space-y-3">
                {recipe.instructions.map((step, index) => (
                  <li key={step} className="flex gap-3">
                    <span className="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-slate-900 text-sm font-semibold text-white">
                      {index + 1}
                    </span>
                    <span className="pt-0.5 leading-6 text-slate-700">{step}</span>
                  </li>
                ))}
              </ol>
            </section>
          </div>

          {recipe.notes && (
            <div className="rounded-2xl border bg-white p-4">
              <h2 className="mb-1 font-semibold">Family Notes</h2>
              <p className="text-slate-600">{recipe.notes}</p>
            </div>
          )}

          <a
            href={recipe.sourceUrl}
            target="_blank"
            rel="noreferrer"
            className="inline-flex items-center gap-2 text-sm font-medium text-slate-700 underline underline-offset-4"
          >
            View original recipe at {recipe.source} <ExternalLink className="h-4 w-4" />
          </a>
        </CardContent>
      </Card>
    </div>
  );
}

export default function FamilyRecipeHub() {
  const [selectedRecipe, setSelectedRecipe] = useState(null);
  const [query, setQuery] = useState("");
  const [category, setCategory] = useState("All");

  const categories = useMemo(() => ["All", ...Array.from(new Set(recipes.map((recipe) => recipe.category)))], []);

  const filteredRecipes = useMemo(() => {
    const normalizedQuery = query.toLowerCase().trim();
    return recipes.filter((recipe) => {
      const matchesCategory = category === "All" || recipe.category === category;
      const searchableText = [
        recipe.name,
        recipe.category,
        recipe.description,
        ...recipe.tags,
        ...recipe.ingredients
      ]
        .join(" ")
        .toLowerCase();
      return matchesCategory && searchableText.includes(normalizedQuery);
    });
  }, [query, category]);

  if (selectedRecipe) {
    return (
      <main className="min-h-screen bg-slate-100 p-4 md:p-8">
        <RecipeDetail recipe={selectedRecipe} onBack={() => setSelectedRecipe(null)} />
      </main>
    );
  }

  return (
    <main className="min-h-screen bg-slate-100 p-4 md:p-8">
      <div className="mx-auto max-w-6xl space-y-8">
        <header className="rounded-3xl bg-white p-6 shadow-sm md:p-8">
          <p className="text-sm font-medium uppercase tracking-wide text-slate-500">Family Recipe Hub</p>
          <div className="mt-3 grid gap-4 md:grid-cols-[1fr_auto] md:items-end">
            <div>
              <h1 className="text-4xl font-bold tracking-tight md:text-5xl">What should we eat this week?</h1>
              <p className="mt-3 max-w-2xl leading-7 text-slate-600">
                A curated family recipe list with quick filters, searchable ingredients, and full recipe pages.
              </p>
            </div>
            <div className="rounded-2xl bg-slate-900 px-5 py-4 text-white">
              <div className="text-3xl font-bold">{recipes.length}</div>
              <div className="text-sm text-slate-300">saved recipes</div>
            </div>
          </div>
        </header>

        <section className="grid gap-3 rounded-3xl bg-white p-4 shadow-sm md:grid-cols-[1fr_auto]">
          <div className="relative">
            <Search className="absolute left-3 top-1/2 h-5 w-5 -translate-y-1/2 text-slate-400" />
            <input
              value={query}
              onChange={(event) => setQuery(event.target.value)}
              placeholder="Search by recipe, ingredient, or tag..."
              className="w-full rounded-2xl border bg-white py-3 pl-10 pr-4 outline-none ring-slate-300 focus:ring-2"
            />
          </div>
          <select
            value={category}
            onChange={(event) => setCategory(event.target.value)}
            className="rounded-2xl border bg-white px-4 py-3 outline-none ring-slate-300 focus:ring-2"
          >
            {categories.map((item) => (
              <option key={item} value={item}>{item}</option>
            ))}
          </select>
        </section>

        <section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
          {filteredRecipes.map((recipe) => (
            <RecipeCard key={recipe.id} recipe={recipe} onSelect={setSelectedRecipe} />
          ))}
        </section>

        {filteredRecipes.length === 0 && (
          <div className="rounded-3xl bg-white p-8 text-center text-slate-600 shadow-sm">
            No recipes found. Try a different search or category.
          </div>
        )}
      </div>
    </main>
  );
}
